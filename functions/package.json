{
  "name": "reop-audit-functions",
  "description": "مستكشف الفرص العقارية — Cloud Functions لسجل التدقيق غير القابل للتلاعب ومزامنة قائمة انتظار Monday.com الآمنة عبر Admin SDK.",
  "private": true,
  "type": "commonjs",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build:domain": "node scripts/build-domain.cjs",
    "check:domain": "node scripts/build-domain.cjs --check",
    "lint": "node --check index.js && node --check trusted-ic.cjs && npm run check:domain",
    "test": "npm run check:domain && node test/p0-trusted-transaction-layer.test.js && node test/trusted-ic.test.cjs",
    "test:p0": "node test/p0-trusted-transaction-layer.test.js",
    "deploy": "npm run build:domain && npm test && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "nodemailer": "^10.0.10"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
